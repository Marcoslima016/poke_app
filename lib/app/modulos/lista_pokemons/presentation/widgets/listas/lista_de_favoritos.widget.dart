import 'package:flutter/material.dart';

class ListaDeFavoritos extends StatelessWidget {
  // const ListaDeFavoritos({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
